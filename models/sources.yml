version: 2

sources:
  - name: raw
    # TODO: Replace with your source database and schema
    database: RAW_DB
    schema: RAW_SCHEMA
    tables:
      # TODO: Add your source tables below. Example:
      # - name: orders
      #   description: "Customer orders with fulfillment status"
      # - name: products
      #   description: "Product catalog"
      # - name: inventory_snapshots
      #   description: "Daily inventory position snapshots"
      - name: placeholder_table
        description: "TODO: Replace with your first source table"
